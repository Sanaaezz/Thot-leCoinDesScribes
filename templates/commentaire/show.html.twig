{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    
<header>
    <div>
        <img src="" alt="">
    </div>
    <h1>Commentaire</h1>
</header>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Titre_commentaire</th>
                <td>{{ commentaire.titreCommentaire }}</td>
            </tr>
            <tr>
                <th>Texte_commentaire</th>
                <td>{{ commentaire.texteCommentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
